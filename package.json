{
  "name": "clown-tunes",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.10.12"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
